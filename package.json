{
  "name": "telegram-channel-archiver",
  "version": "0.1.0",
  "description": "Chrome extension for archiving media from Telegram Web K channels.",
  "license": "MIT",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "build:watch": "node scripts/build.mjs --watch",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "@types/node": "^20.12.7",
    "esbuild": "^0.21.0",
    "fake-indexeddb": "^6.2.5",
    "typescript": "^5.4.5",
    "vitest": "^1.6.0"
  }
}
